export enum PlayerColor {
    Black,
    White
}

export enum PieceName {
    King,
    Queen,
    Rook,
    Bishop,
    Knight,
    Pawn
}

export enum AttackBoardType {
    Left,
    Right
}

export enum FullBoardType {
    Top,
    Middle,
    Bottom
}
