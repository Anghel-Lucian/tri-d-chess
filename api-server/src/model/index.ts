import DBConnection from './DBConnection.js';

/**
  * DBConnection implementation can be replaced as long as it has the same
  * interface, import needs to be updated to use the desired implementation
  */
export default DBConnection;
