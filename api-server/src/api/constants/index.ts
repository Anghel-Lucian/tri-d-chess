export const APIS = {
    SIGN_IN: "sign-in",
    LOG_IN: "log-in",
    STATS: "stats",
    GUEST: "guest"
}

export const API_ROUTES = {
    SIGN_IN: "/" + APIS.SIGN_IN,
    LOG_IN: "/" + APIS.LOG_IN,
    STATS: "/" + APIS.STATS,
    GUEST: "/" + APIS.GUEST
}

export enum HTTP_METHODS {
    GET = "get",
    POST = "post"
}

